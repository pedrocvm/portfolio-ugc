/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // O Next 16 escreve AGENTS.md/CLAUDE.md na raiz por omissão.
  agentRules: false,
};

export default nextConfig;
